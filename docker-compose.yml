services:
  project-a:
    image: hub.devopsinaction.lab/project-a-prod:14.1.26.27
    container_name: project-a-prod
    ports:
      - "3000:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s